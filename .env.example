# Postgres (used by docker-compose and DATABASE_URL)
POSTGRES_USER=emailsolver
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=emailsolver
DATABASE_URL=postgresql+asyncpg://emailsolver:emailsolver@localhost:5432/emailsolver

# JWT
JWT_SECRET_KEY=change-me-to-a-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/api/v1/auth/callback

# Encryption (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEY=change-me-to-a-fernet-key

# Anthropic
ANTHROPIC_API_KEY=your-anthropic-api-key

# App
APP_ENV=development
CLASSIFIED_EMAIL_TTL_DAYS=7

# Domain (used by Caddy for auto-HTTPS and frontend build)
DOMAIN=tidyinbox.net

# Web app URL (used by backend for OAuth redirect validation)
WEB_APP_URL=https://tidyinbox.net

# Google OAuth redirect (update for production)
# GOOGLE_REDIRECT_URI=https://tidyinbox.net/api/v1/auth/callback
